import { render, screen } from '@testing-library/react';
import App from './App';

// test("header render with react testing tutorial in the document", () => {
//   render(<App />);
//   const linkElement = screen.getByText(/This is react testing Tutorial/i);
//   expect(linkElement).toBeInTheDocument();
// });
// test("should check navbar is present", () => {
//   render(<App />);
//   const linkElement = screen.getByTestId('navbar');
//   expect(linkElement).not.toBeNull();
// });
